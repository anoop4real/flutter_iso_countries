name: iso_countries
description: A plugin to get list of ISO Countries from device OS.
version: 1.0.1
author: Anoop M <anoopm6@gmail.com>
homepage: https://github.com/anoop4real/flutter_iso_countries
repository: https://github.com/anoop4real/flutter_iso_countries

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter.
flutter:
  # This section identifies this Flutter project as a plugin project.
  # The androidPackage and pluginClass identifiers should not ordinarily
  # be modified. They are used by the tooling to maintain consistency when
  # adding or updating assets for this project.
  plugin:
    androidPackage: com.anoop4real.iso_countries
    pluginClass: IsoCountriesPlugin

